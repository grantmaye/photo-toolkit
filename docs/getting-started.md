# Getting started

Install locally with `pip install -e .`, then run `photo verify /path/to/staging`.

Start with read-only commands:

```bash
photo verify /path/to/staging
photo report /path/to/staging
photo hash /path/to/staging
```

Then dry-run any planned changes before adding `--execute`.
